# 更新日志

## [v1.2.0] - 待定

### 新增
- 私聊页面头部展示对方头像与用户名（从 profile 缓存解析）
- 私聊消息列表中展示发送者头像（自己与对方均显示）
- 从 URL 直接进入私聊（如 `/friends?dm=dm:xxx:yyy`）时，自动拉取对方资料并补全频道名称与头像

### 修复
- 私聊对方名称改为显示用户名，不再显示邮箱
- 私聊消息中对方昵称优先使用 profile 中的用户名，避免历史消息里存的邮箱或旧昵称
- 聊天场景下自己的头像不更新：上传新头像后，同步逻辑改为监听 `id/username/avatar` 变化，保证 `currentUser` 头像即时更新

### 其他
- Chat 模块引入 profileCache，私聊对方资料统一走缓存，减少重复请求

---

## [v1.1.0] - 待定

### 新增
- **用户资料缓存（profileCache）**：对用户名、头像等做内存 + sessionStorage 缓存（TTL 5 分钟），减少重复请求 profiles 接口
- 登录/恢复 session 时优先从缓存读取当前用户 profile，无缓存再请求接口
- 好友列表、大厅房主/成员、房间内 KP 名称等均走 profileCache，未命中再请求
- 模组列表移除「其他」选项；创建房间时若使用自定义模组名，会自动加入模组选项表，后续可选
- 角色卡列表操作菜单新增「修改头像」，支持在列表页直接上传/更换角色卡头像
- 角色卡详情弹窗（CharacterCardModal）有头像时展示头像，无头像时展示姓名首字

### 修复
- 模组选择（ModuleSelect）去掉 Combobox，改为普通输入框 + 手动下拉，避免自动匹配
- 创建房间时自定义模组被误替换为「亡蝶葬仪」：增加组件外 mousedown 关闭下拉，避免点击「创建」时误触下拉项
- ModuleSelect 中 `showDropdown = true` 误写为对 ref 赋值，导致报错「Assignment to constant variable」，改为 `showDropdown.value = true`

### 其他
- 角色卡编辑页（基础信息）内移除「上传头像」入口，仅保留头像展示与提示「在角色卡列表的操作菜单中可修改头像」
- 角色卡头像上传后写入数据库并更新本地列表，列表项支持显示头像缩略图
- 登出时清空 profileCache；修改昵称/头像后同步更新缓存
- 新增数据库迁移：删除模组选项「其他」、允许已登录用户向模组选项表插入新行

---

## [v1.0.0] - 初始版本

### 新增
- 用户注册、登录（邮箱+密码）
- 个人资料：昵称、头像上传与展示
- 好友：添加好友、请求列表、同意/拒绝/删除
- 大厅（游戏房间）：创建房间、模组与标签筛选、申请加入、房主审核
- 房间内：角色卡选择与审核、模组文件、日志与聊天
- 角色卡：创建/编辑/删除、基础信息与能力、提交审核、角色卡详情弹窗
- 私聊（DM）：从好友列表发起、消息列表与输入、Realtime 新消息
- 侧边栏：频道与模组、私聊列表、用户信息与设置入口
- 通知、笔记等基础页面与路由

### 其他
- 基于 Vue 3 + Supabase（Auth、Database、Storage、Realtime）
- 跑团相关：COC 角色卡、技能与属性、骰子等基础能力

---

[v1.2.0]: https://github.com/your-repo/TRPG/compare/v1.1.0...v1.2.0
[v1.1.0]: https://github.com/your-repo/TRPG/compare/v1.0.0...v1.1.0
[v1.0.0]: https://github.com/your-repo/TRPG/releases/tag/v1.0.0
